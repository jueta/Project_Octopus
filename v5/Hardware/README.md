# v5 Hardware

Use this directory for the active `v5` hardware work:

- enclosure concepts,
- wiring sketches,
- bill of materials,
- mounting ideas,
- power and connector decisions.

Current assumption: `v5` is wired-first and can be smaller because it does not need to prioritize the old battery architecture.
